# AGENTS.md

本文件是 Codex 在本项目中的工作规范。

## 工作原则

1. 每次只完成一个小任务。
2. 不要直接提交 Git。
3. 不要修改 `main` 分支上的代码，除非用户明确要求。
4. 不要提交 `.venv/`、`__pycache__/`、`experiments/results/`、`.env`、`.idea/`。
5. 修改代码后必须更新 `README.md` 或 `notes/` 中对应说明。
6. 新增功能尽量添加简单测试。
7. 不要编造实验结果。
8. 如果脚本没有实际运行成功，必须明确说明。
9. 每次完成后列出修改文件、运行命令、验证结果。
